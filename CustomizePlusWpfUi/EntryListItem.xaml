<UserControl x:Class="CustomizePlusWpfUi.EntryListItem"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:local="clr-namespace:CustomizePlusWpfUi"
			 xmlns:controls="clr-namespace:XivToolsWpf.Controls;assembly=XivToolsWpf"
			 mc:Ignorable="d" 
			 d:DesignHeight="450" d:DesignWidth="800">

	<Grid x:Name="ContentArea">

		<Grid.ColumnDefinitions>
				<ColumnDefinition/>
				<ColumnDefinition Width="Auto"/>
				<ColumnDefinition Width="Auto"/>
			</Grid.ColumnDefinitions>

			<TextBox Text="{Binding Name}" Style="{StaticResource MaterialDesignTextBox}" VerticalAlignment="Center"/>

			<Button Grid.Column="1" Style="{StaticResource TransparentIconButton}" Width="28" IsEnabled="False"
					Click="OnEditClicked">
				<Button.ToolTip>
					<controls:TextBlock Key="Mods_EditSliders"/>
				</Button.ToolTip>

				<controls:IconBlock Icon="SlidersH"/>
			</Button>

			<Button Grid.Column="2" Style="{StaticResource TransparentIconButton}" Width="28" Padding="0"
					Click="OnImportClicked">

				<Button.ToolTip>
					<controls:TextBlock Key="Mods_ImportPose"/>
				</Button.ToolTip>

				<controls:IconBlock Icon="Running"/>
			</Button>

		</Grid>
</UserControl>
